package globals;

/**
 * This class holds our program globals. While globals are generally not good practice, for
 * the purposes of this small program, it allows us to specify default values that are relevant
 * to the entire project, while avoiding the use of "magic numbers". In a real-world scenario,
 * the number of results would be defined programmatically by the method that is using it.
 */
public class Globals {
  public static final int NUM_RESULTS = 3;
}
